# PostgreSQL
POSTGRES_USER=ecoescom
POSTGRES_PASSWORD=cambia_esto

# API
JWT_SECRET=cambia_esto_por_algo_largo_y_aleatorio
JWT_EXPIRES_IN=7d

# SMTP (opcional)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
MAIL_FROM=EcoESCOM <no-reply@ecoescom.mx>

# Web (URL pública de la API — se usa en el navegador)
NEXT_PUBLIC_API_URL=https://api.tudominio.com/api
